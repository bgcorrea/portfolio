[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x"]

[phases.install]
cmds = ["cd backend && pnpm install --frozen-lockfile"]

[phases.start]
cmd = "cd backend && pnpm run start"

[variables]
NODE_ENV = "production"
